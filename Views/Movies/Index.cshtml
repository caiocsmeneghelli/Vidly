@model List<Vidly.Models.Movie>
@{
    ViewBag.Title = "Movies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-condensed">
    <thead>
        <tr>
            <th><h2>Movies</h2></th>
            <th><h2>Genre</h2></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count == 0)
        {
            <tr>
                <td>We don't have any movies yet.</td>
            </tr>
        }
        else
        {
            foreach(var movie in Model)
                {
                    <tr>
                        <td>
                            <h4>@Html.ActionLink(movie.Name, "Edit", "Movies", new { id = movie.Id }, null)</h4>
                        </td>
                        <td>
                            <h4>@movie.Genre.Name</h4>
                        </td>
                    </tr>
                }
        }
    </tbody>
</table>

